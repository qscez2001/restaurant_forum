<div class="container">
  <div class="row">

    <!-- 使用 Bootstrap components 的 Nav Tabs -->
    <!-- https://getbootstrap.com/docs/3.3/components/#nav-tabs -->
    <ul class="nav nav-tabs">
      <li role="presentation" class="active">
        <%= link_to '首頁', root_path %>
      </li>
      <li role="presentation">
        <%= link_to '最新動態', feeds_restaurants_path %>
      </li>
      <li role="presentation">
        <%= link_to 'Top 10 人氣餐聽', ranking_restaurants_path %>
      </li>
    </ul>

  </div>
</div>

<br>

<div class="container">
  <div class="row">

    <div class="col-md-10 col-md-offset-1">
      <h1>Top 10 人氣餐聽</h1>
      <hr>
      <div class="panel panel-default">
          <div class="panel-body">
            <% @hot_restaurants.each do |restaurant| %>
              <div>
                <h4>
                  <%= link_to restaurant.name, restaurant_path(restaurant) %>
                  <!-- <small><%= restaurant.category.name %></small> -->
                  <small><%= restaurant.favorites_count %></small>
                </h4>
                <p><%= truncate(restaurant.description, length: 80) %></p>
                <hr>
              </div>
            <% end %>
          </div>
        
      </div>
    </div>
  </div>
</div>